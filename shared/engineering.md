# 簡單優先

用最少的程式碼解決問題，不寫投機性的東西：

- 不加沒被要求的功能與「彈性」。
- 單次使用的程式碼不抽象化；三行重複勝過過早抽象。
- 寫了 200 行但 50 行能解決，就重寫。

# 外科手術式修改

- 只碰必要的地方，不順手改相鄰程式碼的註解與格式。
- 自己的修改造成的孤兒（import／變數／函式）要清乾淨；既有的 dead code 不動，只提出。

# 目標驅動

開工前把任務轉成可驗證的目標——「修 bug」→「寫一個重現它的測試，然後讓它通過」。
